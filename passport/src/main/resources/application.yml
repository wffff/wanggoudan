spring:
  datasource:
    url: jdbc:postgresql://118.25.193.38:5432/postgres
    username: postgres
    password: 123
    driver-class-name: org.postgresql.Driver
  mvc:
    static-path-pattern: /static/**
  thymeleaf:
    mode: HTML5
    encoding: UTF-8
    cache: false

server:
  port: 8080
  tomcat:
    uri-encoding: UTF-8

logging:
  level:
    root: WARN
    org.springframework.web: INFO
    org.springframework.security: INFO
